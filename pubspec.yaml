name: dr_cash_clinics
description: Dr Cash Clinics

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #COMMON
  http: ^0.13.3
  shared_preferences: ^2.0.6
  url_launcher: ^6.0.20

  # STATE MANAGEMENT
  mobx: ^2.0.3
  flutter_mobx: ^2.0.1

  #LOCATION
#  geolocator: 8.0.5
#  geocoder: ^0.2.1
  location: ^4.2.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
